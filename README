galerie:  A private photo share service.

$ rake db:migrate
# ruby script/console
Loading development environment (Rails 2.1.0)
>> user = User.new( :name => 'USERNAME', :password => 'PASSWORD', :password_confirmation => 'PASSWORD' )
>> user.save
$ ruby script/server

Access http://localhost:3000 by browser.

enjoy!
